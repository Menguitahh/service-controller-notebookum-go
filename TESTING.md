# Guía de Testing Manual — Service Controller NotebookUm

El controller corre en `http://localhost:5000`.

Levantarlo:

```bash
docker compose up --build -d
```

## Tests automáticos

```bash
go test ./...
```
